services:
    app:
        build:
            context: .
            args:
                VITE_APP_NAME: ${APP_NAME}
        restart: unless-stopped
        ports:
            - '8080:8000'
        env_file:
            - .env
        volumes:
            - app_storage:/var/www/html/storage

volumes:
    app_storage:
